# InkyPi runtime dependencies — loose constraints for pip-compile.
# Edit this file to add/upgrade dependencies, then regenerate:
#   pip-compile --generate-hashes install/requirements.in -o install/requirements.txt
#
# Packages with sys_platform guards are only installed on Linux (Pi).

flask>=3.1,<4
python-dotenv>=1.0,<2
# inky display driver — only needed on Pi hardware
inky>=2.3,<3; sys_platform == "linux"
requests>=2.32,<3
urllib3>=2.0,<3
werkzeug>=3.1,<4
pillow>=11.0,<13
# pi-heif has wheels for all major platforms (Linux, macOS, Windows) — no platform guard.
pi-heif==1.4.0
tzdata>=2024.1
openai>=2.0,<3
google-genai>=1.14,<3
numpy>=2.0,<3
icalendar>=6.0,<7
recurring-ical-events>=3.0,<4
# homeboard gsheets adapter (trips, home_maintenance) — read-only Sheets
# access via a service account. google-auth is already pulled in
# transitively by google-genai; pinned directly here since we now import it.
google-api-python-client>=2.0,<3
google-auth>=2.0,<3
psutil>=7.0,<8
# cysystemd — only available on Linux; provides journald integration
cysystemd>=2.0,<3; sys_platform == "linux"
waitress>=3.0,<4
feedparser>=6.0,<7
jsonschema>=4.0,<5
astral>=3.0,<4
prometheus-client>=0.21,<1
